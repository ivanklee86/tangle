apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tangle.fullname" . }}-test-connection"
  labels:
    {{- include "tangle.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tangle.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
