apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: argocd-server-external
spec:
  parentRefs:
    - name: traefik-gateway
  rules:
    - backendRefs:
        - name: argocd-server
          port: 80
