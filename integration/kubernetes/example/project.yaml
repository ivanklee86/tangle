apiVersion: argoproj.io/v1alpha1
kind: AppProject
metadata:
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  name: my-project
spec:
  clusterResourceWhitelist:
    - group: '*'
      kind: '*'
  description: agnostic-dev01 project
  destinations:
    - namespace: '*'
      server: '*'
  sourceRepos:
    - '*'
